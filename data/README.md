# Data

Datasets are not stored in this repository.

- `raw/` — original downloaded datasets
- `processed/` — preprocessed/generated datasets

These directories are excluded from Git because datasets can be large.