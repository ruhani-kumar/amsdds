[pytest]
# Only collect the application test suite. The handover drop under
# handover/amsdds_backend/ ships its own test_pipeline.py, which constructs
# the real Engine (and loads weights) at import time - it is a manual smoke
# script, not a pytest test, and must not be collected here.
testpaths = tests
