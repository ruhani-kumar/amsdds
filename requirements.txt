# ---------------------------------------------------------------------------
# Application shell (backend + frontend + tests)
# ---------------------------------------------------------------------------
Flask>=2.3,<4
streamlit>=1.30
requests>=2.31
PyYAML>=6.0
Pillow>=10.0
pytest>=7.4

# ---------------------------------------------------------------------------
# Real ML pipeline (handover engine: backend/ml_core/). Required ONLY for real
# inference - DEVELOPMENT_MOCK_MODE, /health, and the mock test suite run
# without these.
# ---------------------------------------------------------------------------
torch>=2.2
torchvision>=0.17
numpy>=1.24
timm>=1.0                  # PanDerm BEiT ViT-B/16 (Layer 2, default backbone)
transformers>=4.46,<6      # RT-DETR R50vd encoder (Layer 2 fallback/reference)
flask-cors>=4              # handover API parity (not yet enabled in backend/app.py)

# Training / evaluation code in ml/ only (not used by the backend at inference).
# scikit-learn>=1.3   # ml/preprocessing/splits.py
# pandas>=2.0         # ml/preprocessing ; handover test_pipeline.py --parity
huggingface_hub>=0.30
